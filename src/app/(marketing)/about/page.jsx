import TitlePage from "@/components/pagesComponents/TitlePage";
// el arroba identifica a la carpeta src
// la ruta la hace genérica y se puede utilizar esa misma ruta en cualquier otra página o componente

const About = () => {
    return <TitlePage title="About" />;
};

export default About;
